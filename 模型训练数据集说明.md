# DeepfakeBench 模型训练数据集总结

基于对配置文件和GitHub仓库的分析，以下是当前WebUI中保留的各个模型的训练数据集信息：

## 📊 训练数据集概述

根据DeepfakeBench的官方说明和配置文件，**所有提供的预训练模型权重都是在 FaceForensics++ (FF++) 数据集上训练的**，但具体的训练子集有所不同。

---

## 🎯 各模型的训练数据集详情

### **1. ResNet34 (CNN-Aug)**
- **训练数据集**: `FF-NT` (FaceForensics++ 的 NeuralTextures 子集)
- **Backbone**: ResNet34 (from ImageNet pretrained)
- **用途**: 基础的卷积神经网络增强模型

### **2. EfficientNet-B4**
- **训练数据集**: `FF-NT` (FaceForensics++ 的 NeuralTextures 子集)
- **Backbone**: EfficientNet-B4 (from ImageNet pretrained)
- **特点**: 高效的网络架构，适合资源受限环境

### **3. Capsule (胶囊网络)**
- **训练数据集**: **多数据集联合训练**
  - `FaceForensics++` (完整数据集)
  - `Celeb-DF-v2`
  - `DFDCP`
  - `DeepFakeDetection`
- **Backbone**: Xception
- **特点**: 使用胶囊网络架构，具有更好的泛化能力

### **4. CORE**
- **训练数据集**: `FF-NT` (FaceForensics++ 的 NeuralTextures 子集)
- **Backbone**: Xception
- **特点**: 使用一致性正则化损失 (consistency_loss)
- **优势**: 在UADFV数据集上表现最佳

### **5. UCF (Xception)**
- **训练数据集**: **多子集联合训练**
  - `FF-F2F` (Face2Face)
  - `FF-DF` (DeepFakes)
  - `FF-FS` (FaceSwap)
  - `FF-NT` (NeuralTextures)
- **Backbone**: Xception (adjust_channel mode)
- **特点**: 
  - 使用配对数据集 (dataset_type: pair)
  - 多种损失函数组合 (分类损失、特征损失、对比正则化、重建损失)
  - **在Celeb-DF-v1上表现最佳**

### **6. SPSL**
- **训练数据集**: `FF-FS` (FaceForensics++ 的 FaceSwap 子集)
- **Backbone**: Xception (4通道输入, inc: 4)
- **特点**: 
  - 使用频域特征
  - 训练时只使用4帧 (frame_num: train=4, test=32)
  - **在高质量数据集上表现优异**

### **7. SRM**
- **训练数据集**: `FF-NT` (推测，配置文件中未明确指定)
- **特点**: 使用频域分析方法

---

## 📚 主要数据集说明

### **FaceForensics++ (FF++) 数据集**
FF++是最常用的Deepfake检测训练数据集，包含以下4个主要的伪造技术子集：

1. **FF-F2F (Face2Face)** - 面部表情转移
2. **FF-DF (DeepFakes)** - 基于自动编码器的换脸
3. **FF-FS (FaceSwap)** - 传统的换脸技术
4. **FF-NT (NeuralTextures)** - 基于神经纹理的面部操控

**压缩等级**: c23 (所有模型统一使用)
- c23 表示中等压缩质量，更接近实际应用场景

### **其他训练数据集**
- **Celeb-DF-v2**: 高质量的名人Deepfake视频数据集
- **DFDCP**: Deepfake Detection Challenge Preview
- **DeepFakeDetection (DFD)**: Google/Jigsaw发布的检测数据集

---

## 🎓 训练策略总结

### **单一数据集训练**
- ResNet34, EfficientNet-B4, CORE, SRM, SPSL
- 优点: 训练快速，在特定类型伪造上效果好
- 缺点: 泛化能力相对较弱

### **多数据集联合训练**
- **Capsule**: 4个数据集联合
- **UCF**: 4个FF++子集联合
- 优点: **更强的跨数据集泛化能力**
- 缺点: 训练时间更长

---

## 💡 关键发现

1. **UCF在高质量数据集上表现最佳**的原因：
   - 使用了FF++所有4个子集进行训练
   - 采用了复杂的多任务学习框架
   - 使用配对数据进行对比学习

2. **CORE在简单数据集(UADFV)上表现最佳**的原因：
   - 使用了一致性正则化
   - 对特定类型的伪造（如NN-based）特别敏感

3. **Capsule泛化能力强**的原因：
   - 在多个不同来源的数据集上训练
   - 胶囊网络架构本身就设计用于提高泛化能力

---

## 🔍 推理

根据GitHub仓库的说明：
> "we train the detectors on the FF++ (c23) dataset and assess their performance on other datasets."

**预训练权重的标准训练流程**：
1. 使用ImageNet预训练的Backbone（ResNet, EfficientNet, Xception等）
2. 在FaceForensics++ (c23) 数据集上进行Deepfake检测微调
3. 在多个测试数据集上进行跨域评估（Cross-domain evaluation）

这也解释了为什么：
- 在训练数据集（FF++）上，所有模型AUC都能达到95%+
- 在跨域数据集（Celeb-DF, UADFV）上，性能会有明显下降
- 使用多数据集训练的模型（UCF, Capsule）在跨域评估时表现更稳定

---

## 📖 参考资料
- 论文: DeepfakeBench: A Comprehensive Benchmark of Deepfake Detection (NeurIPS 2023)
- GitHub: https://github.com/SCLBD/DeepfakeBench
- 配置文件位置: `./training/config/detector/*.yaml`
